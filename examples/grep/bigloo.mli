(* Additional operations, without sanity checks *)

(* This module implements extension to the caml-light librarie. *)

(*--*)

value char_to_int : char -> int = 1 "char_to_int"
  and int_to_char : int -> char = 1 "int_to_char"
;;
